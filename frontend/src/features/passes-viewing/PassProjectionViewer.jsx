import {Stage, Layer, Text} from "react-konva";

const PassProjectionViewer = () => {
    return <Stage width={500} height={500}>
        <Layer>
            <Text text="jopaaaa" fontSize={15} fill="#ffffff"/>
        </Layer>
    </Stage>;
};

export default PassProjectionViewer;